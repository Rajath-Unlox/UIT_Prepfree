import LoginPage from './login/page';
/**
 * This page serves as the root entry point (www.prepfree.com).
 * It imports and renders the LoginPage component to cover the base URL,
 */
export default function RootPage() {
  return <LoginPage />;
}